### This use case is to create an intermediate model and a simplified version of DM_XING_KPI table with a small subset of KPIs and dimensions.

### The queries are not adapted to Snowflake syntax, also table names are from on-premise database which should be replaced with references to the 'to be created' models.