### This use case is to create a simplified version of F_JOB_POSTING table with a small subset of dimensions.

### The query is already adapted to Snowflake syntax and working fine with the existing tables.